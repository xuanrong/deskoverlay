# Windows 桌面 HTML 工作台 — 设计文档

> 代号：**DeskOverlay**
> 版本：v0.1（技术验证 Spike 阶段）
> 日期：2026-08-15
> 状态：架构与可行性已通过 Spike 实现验证骨架；Spike 编译收敛中（见 §12）。

---

## 1. 背景与问题陈述

市面上的「桌面整理」工具（典型如 Stardock Fences）本质是**桌面图标管理器**：在桌面上画几个围栏框，把 `.lnk` / 文件夹归拢进去。它们并不接管桌面本身，用户看到的仍是系统 Explorer 渲染的图标与壁纸。

本产品的出发点不同：我们想要的不是一个「整理图标的工具」，而是一个**把 HTML 工作台直接覆盖到 Windows 桌面之上、让 HTML 成为桌面本身**的运行时。换句话说——

> **桌面 = 一个全屏的 WebView2 画布，上面承载可拖拽、可缩放、可交互的 HTML 面板；原生桌面图标被隐藏，真实桌面只在面板之外的空白处「穿透」可见、可右键。**

调研结论（见 §13）：这一形态目前是**市场空白**。最接近的 PureDesk 是「WebView2 浮动面板」，并非桌面接管，且为闭源商业软件；开源侧没有任何项目同时满足「全屏接管 + 隐藏原生图标 + 全交互面板」三点。

---

## 2. 产品定位与设计原则

### 2.1 已锁定的关键决策
| 维度 | 决策 | 说明 |
| --- | --- | --- |
| 覆盖范围 | **全屏接管（HTML 即桌面）** | 不是浮动面板，是整块桌面的替代层 |
| 技术栈 | **Tauri（Rust + WebView2）** | 轻量、原生窗口控制力强、前端即 Web 技术栈 |
| 原生图标 | **隐藏原生图标** | 由覆盖层承载工作台，原生图标不显示 |
| 交互形态 | **全交互型** | 面板可拖拽、可缩放、可点击，非只读展示 |

### 2.2 设计原则（沿用用户偏好）
- **干净、简洁、实用**：工作台信息密度高但不花哨，功能优先。
- **影院感 / 高级质感**（视觉倾向）：面板与装饰采用深色底 + 克制的高光，避免廉价感；但本效率工具以「可读、可用」为第一优先级。
- **可迭代**：配置驱动，面板与 Provider 均可由数据描述，便于后续按反馈快速调整。

---

## 3. 核心交互范式

```
┌─────────────────────────── 真实桌面（被覆盖，仅在空白处穿透） ───────────────────────────┐
│                                                                                          │
│   ┌──────────────┐   ┌──────────────┐                                                    │
│   │  CPU 面板     │   │  待办面板     │      ← 可拖拽 / 可缩放 / 可交互（命中 HTCLIENT）   │
│   │  23%         │   │  □ 写文档    │                                                    │
│   └──────────────┘   └──────────────┘                                                    │
│                                                                                          │
│         ← 面板外区域：点击/右键穿透到下方 Explorer 桌面（命中 HTTRANSPARENT） →            │
│                                                                                          │
└──────────────────────────────────────────────────────────────────────────────────────────┘
       承载层 = 单块透明全屏 WebView2，作为桌面子窗口（WS_CHILD / WS_EX_TOOLWINDOW）
```

四条交互契约：
1. **不可见性**：程序不出现在任务栏 / Alt-Tab（`WS_EX_TOOLWINDOW`）。
2. **选择性穿透**：面板矩形内可交互；矩形外点击「落」到真实桌面（可右键、可选中图标——若图标未隐藏）。
3. **面板即组件**：每个面板是一个独立 HTML 容器，可挂不同内容（系统指标、笔记、快捷入口…）。
4. **自愈**：Explorer 重启后自动重新挂回桌面，用户无感。

---

## 4. 总体架构

Tauri 2 天然提供「Rust 后端 + Web 前端」双层结构，正好对应本产品的两类职责：

```
┌──────────────────────────── 前端（WebView2 / HTML+JS） ────────────────────────────┐
│  工作台渲染层                                                                         │
│   · 透明全屏画布                                                                      │
│   · 面板组件（拖拽 / 缩放 / 内容）                                                    │
│   · 监听后端事件（provider-emit）刷新数据                                            │
│        │  invoke（命令：set_clickthrough / report_panel_rect）                       │
│        │  listen（事件：provider-emit）                                              │
└────────┬───────────────────────────────────────────────────────────────────────────┘
         │  Tauri IPC
┌────────┴──────────────────────────── 后端（Rust） ─────────────────────────────────┐
│  main.rs（setup 编排）                                                               │
│   ├─ desktop_inject  桌面窗口注入 + Explorer 自愈                                    │
│   ├─ window_subclass  WM_NCHITTEST 选择性穿透                                        │
│   └─ sys_bridge       系统数据 Provider（CPU…）→ provider-emit                       │
│         │  Win32 API（windows-rs 0.58）                                              │
│         ▼                                                                            │
│  Windows 桌面窗口树（Progman / WorkerW / SHELLDLL_DefView / SysListView32）          │
└───────────────────────────────────────────────────────────────────────────────────┘
```

**前端 ↔ 后端桥接约定**
- 前端 → 后端：**Tauri `invoke`**（命令式调用，如切换穿透、上报面板矩形）。
- 后端 → 前端：**Tauri `event`**（流式推送，如 `provider-emit` 携带 `{config_hash, output}`）。
这一约定直接移植自 Zebar 的 Provider 模型（见 §6.5），保证数据面与命令面分离、可水平扩展。

---

## 5. 关键技术机制（可行性核心）

这是本产品区别于「浮动面板」类工具的真正壁垒，也是 Spike 要验证的三点。

### 5.1 桌面窗口注入（把画布挂到桌面上）
Windows 桌面是一棵窗口树：`Progman → SHELLDLL_DefView → SysListView32`（图标层），其下还有 `WorkerW`（壁纸层）。
1. `FindWindowW("Progman", …)` 取桌面根。
2. 向 `Progman` 发送未公开消息 `0x052C`（`SendMessageTimeoutW`），触发 Explorer 在其后生成一个 `WorkerW`  worker 窗口。
3. 用 `EnumWindows` 找到该 `WorkerW`，作为新的「桌面目标」。
4. 对我们的覆盖窗口设置 `WS_CHILD`，并 `SetParent` 挂到该桌面目标之下；加 `WS_EX_TOOLWINDOW` 使其脱离任务栏。
5. **Win11 24H2「高桌面」回退**：当检测到 `Progman` 带 `WS_EX_NOREDIRECTIONBITMAP` 时，改为挂到 `Progman` 自身、并用 `SetWindowPos` 置于 `SHELLDLL_DefView` 之下，避免被新桌面合成层遮挡。

### 5.2 选择性点击穿透（本产品招牌能力）
- 覆盖窗口整体设 `WS_EX_LAYERED`（透明）。
- 关键：**不用整窗 `WS_EX_TRANSPARENT`**——那样整窗不可点击，面板也废了。
- 改为对窗口（含 WebView2 子窗口）做 **`WM_NCHITTEST` 子类化**：
  - 光标落在已知面板矩形内 → 返回 `HTCLIENT`（交给 WebView2，可交互）。
  - 落在面板矩形外 → 返回 `HTTRANSPARENT`（点击穿透到下方真实桌面）。
- **命中矩形同步**：前端面板被拖拽时，通过 `invoke("report_panel_rect", {x,y,w,h})` 把最新矩形回报给 Rust，Rust 更新命中测试用的矩形集合，保证「拖到哪、哪可点」始终一致。

### 5.3 Explorer 重启自愈
- 独立线程每 ~2s 轮询：重找桌面目标、若窗口已被 Explorer 拆离则重新 `SetParent` 挂回，并在必要时重载 WebView2 内容。
- 效果：任务管理器结束 `explorer.exe` 再启动，覆盖层自动归位，无需重启本程序。

### 5.4 多显示器覆盖
- 用 `GetSystemMetrics(SM_X/SM_Y/SM_CXVIRTUALSCREEN/SM_CYVIRTUALSCREEN)` 取虚拟屏并集矩形，单块 WebView2 覆盖整块虚拟屏。
- **已知限制**：单窗口方案下，若各显示器 DPI/缩放不同或需要「每屏独立桌面背景」，需改为「每屏各注入一个 WorkerW + 一个 WebView」（见 §10 / §11 P1）。

### 5.5 系统数据 Provider 模型（移植自 Zebar）
- 后端 `sys_bridge` 起一个线程，用 `sysinfo` 周期采集（如 CPU 每秒一次）。
- 经 Tauri 事件把 `{config_hash:"cpu", output:{usage}}` 推给前端；前端 `listen("provider-emit")` 后按 `config_hash` 路由到对应面板。
- 该模型可无侵入扩展：新增 RAM / 网络 / 电池 / 日历等 Provider，只需在后端加采集线程、前端加对应 `config_hash` 渲染即可。

---

## 6. 模块设计

| 模块 | 文件 | 职责 |
| --- | --- | --- |
| 入口编排 | `src-tauri/src/main.rs` | `setup` 中依次启动 CPU Provider、适配器（虚拟屏适配 / 取 HWND / 找桌面目标 / 注入 / 子类化 / 记录父窗口 / 启动 Explorer 看门狗）；注册 `invoke` 命令。 |
| 桌面注入 | `src-tauri/src/desktop_inject.rs` | `get_desktop_target`（0x052C + WorkerW 查找 + 24H2 回退）、`embed_in_desktop`（WS_EX_TOOLWINDOW / WS_CHILD / SetParent）、`start_explorer_watcher`（2s 自愈）。 |
| 穿透子类 | `src-tauri/src/window_subclass.rs` | `subclass_proc` 拦截 `WM_NCHITTEST`，按面板矩形返回 `HTCLIENT`/`HTTRANSPARENT`；`report_panel_rect` 命令更新命中矩形。 |
| 数据桥 | `src-tauri/src/sys_bridge.rs` | `start_cpu_provider` 线程 + `provider-emit` 事件推送。 |
| 前端工作台 | `frontend/index.html` + `frontend/main.js` | 透明画布 + 绝对定位面板（显示 CPU%）+ 面板拖拽并回报表矩形。 |
| 开发服务器 | `dev-server.js` | 无依赖 Node 静态服务器，把 `frontend/` 挂到 `http://localhost:1420`，`tauri dev` 的开发期入口。 |

---

## 7. 配置与数据模型（设计目标，Spike 暂硬编码）

后续产品化将由数据驱动，草案 schema：

```jsonc
{
  "desktop": {
    "hide_native_icons": true,      // 隐藏原生图标
    "monitor_strategy": "virtual",  // virtual | per-monitor
    "self_heal": true               // Explorer 重启自愈
  },
  "panels": [
    {
      "id": "cpu",
      "x": 40, "y": 40, "w": 360, "h": 240,
      "z": 10,
      "click_through_outside": true,// 面板外穿透
      "component": "system-metric",
      "bind": { "provider": "cpu" }
    }
  ],
  "providers": [
    { "config_hash": "cpu", "kind": "cpu", "interval_ms": 1000 }
  ]
}
```

---

## 8. 安全与权限模型

- **Capabilities**（`src-tauri/capabilities/default.json`）：窗口 `main` 授予
  `core:default` + `core:event:default` + `core:window:default` + `core:webview:default`
  + 自定义命令 `allow-set-clickthrough` + `allow-report-panel-rect`。
- **窗口属性**：`decorations:false`、`transparent:true`、`resizable:false`；运行时加 `WS_EX_TOOLWINDOW` 使其不可见于任务栏 / Alt-Tab。
- **约束**：自定义命令权限需由 `tauri-build` 在编译期 codegen 生成（见 §12 当前状态）。

---

## 9. 竞品与参考（调研结论）

| 项目 | 类型 | 与本产品的区别 |
| --- | --- | --- |
| **PureDesk** | 闭源商业，WebView2 浮动面板 | 仅浮动面板，**非桌面接管**；闭源不可参考实现。 |
| **Zebar** | 开源（Tauri 2） | Provider 模型标杆，本产品数据桥直接借鉴其 `provider-emit` 模式；但 Zebar 是任务栏组件，无桌面注入/穿透。 |
| **Ivy-Windows-Wallpaper** | 开源 | `Progman`/`WorkerW` 注入的成熟实现参考，本产品注入逻辑借鉴其 `GetDesktopTarget`/`EmbedInDesktop`。 |
| **FloatView** | 开源 | 点击穿透实现参考，本产品在其基础上演进为「选择性穿透」（面板内可交互）。 |

结论：**没有任何开源/闭源项目同时满足「全屏接管 + 隐藏原生图标 + 全交互面板」**，本产品占据空白细分。

---

## 10. 已知限制与风险

1. **多显示器**：当前单窗口覆盖虚拟屏；每屏独立 WorkerW 注入（DPI 各异场景）尚未实现。
2. **Win11 24H2 高桌面**：回退路径为实验性，需真机多版本验证。
3. **整窗透明 + WebView2**：大面积透明层的合成开销需实测；复杂面板可能吃 GPU。
4. **安全软件**：`SetParent` 注入系统桌面可能被部分杀软误报，发布侧需代码签名 + 白名单沟通。
5. **原生图标隐藏**：Spike 通过「覆盖层遮住」实现视觉隐藏；真正「停用 SysListView32 渲染」的稳健方案待 P3。
6. **编译收敛**：Spike 依赖已编译通过，自定义命令权限 codegen 待修复（见 §12）。

---

## 11. 路线图

- **P0 — Spike（当前）**：验证三点可行性（注入 / 选择性穿透 / 自愈）+ CPU Provider + 单显示器覆盖。
- **P1 — 面板系统**：配置驱动的多面板网格（持久化到 §7 schema）；多显示器每屏注入；面板缩放手柄。
- **P2 — Provider 套件**：RAM / 网络 / 电池 / 日历等 Provider；面板模板库；主题系统（深色 + 金色高光装饰框）。
- **P3 — 产品化**：原生图标稳健隐藏；设置面板；自动更新；代码签名与杀软沟通；性能与功耗调优。

---

## 12. 验证状态与计划

### 12.1 Spike 文件清单（位于 `desk-overlay-spike/`）
- `Cargo.toml`（workspace）、`package.json`、`README.md`
- `dev-server.js`、`frontend/index.html`、`frontend/main.js`
- `src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml`、`src-tauri/build.rs`
- `src-tauri/capabilities/default.json`、`src-tauri/icons/icon.ico`
- `src-tauri/src/main.rs`、`desktop_inject.rs`、`window_subclass.rs`、`sys_bridge.rs`
- `.cargo/config.toml`（rsproxy 镜像，仅本项目）

### 12.2 本机验证清单（对照 README）
1. 启动后程序不出现在任务栏 / Alt-Tab（`WS_EX_TOOLWINDOW` 生效）。
2. 面板**外**点击桌面空白处，右键菜单正常弹出（穿透成功）。
3. 拖拽面板可移动，且始终可点击（选择性命中生效）。
4. 任务管理器结束 `explorer.exe` 再重启，画面自动重嵌（自愈）。
5. 多显示器下是否全覆盖（单窗口为已知限制）。
6. 面板实时显示 CPU%。

### 12.3 编译状态（截至本文档）
- 环境已就绪：Tauri CLI（`@tauri-apps/cli@2.11.4`）经 `npm install` 安装；正确运行入口为 `npm run dev`（非 `cargo tauri dev`）。
- 依赖下载：初测 crates.io 被限，已加 rsproxy 镜像（`.cargo/config.toml`），依赖全部下载并编译通过。
- 我们的 crate：已开始编译；当前唯一阻塞为 **Tauri 2 自定义命令权限 codegen**（`allow-set-clickthrough` / `allow-report-panel-rect` 未被生成），修复后即可 `cargo check` 全绿并 `npm run dev` 拉起。该修复与本文档并行处理。

---

## 13. 术语表
- **WorkerW / Progman**：Windows 桌面窗口树中的系统窗口类。
- **0x052C**：触发 Explorer 生成 WorkerW 的非公开窗口消息。
- **HTCLIENT / HTTRANSPARENT**：`WM_NCHITTEST` 返回值，决定点击是「交给本窗口」还是「穿透到下层」。
- **Provider**：本产品中对「系统数据采集器」的统称（移植 Zebar 命名）。
- **codegen**：Tauri 在编译期为 `#[tauri::command]` 自动生成权限与绑定代码的过程。
